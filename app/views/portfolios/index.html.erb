<div class="album text-muted">
  <div class="container">
    <div class="row <%= "sortable" if logged_in?(:site_admin) %>" >
      <%= render partial: 'portfolio_items', collection: @portfolio_items %>
    </div>
  </div>
</div>
