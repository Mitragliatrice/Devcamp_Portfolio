    <% if logged_in?(:site_admin) %>
    <nav class="blogcrumb breadcrumb">
        <%= link_to blog_status_symbol(blog), toggle_status_blog_path(blog) %>
        <%= link_to fa_icon('pencil-square-o'), edit_blog_path(blog)%>
        <%= link_to fa_icon('trash'), blog, method: :delete, data: {confirm: 'Are you sure? Double check!'}%>
    </nav>
    <% end %>